{
	"name": "pharos-controllers",
	"version": "2.0.0",
	"description": "Package to make interacting with the Pharos controller via the HTTP rest API easier.",
	"main": "lib/index.js",
	"scripts": {
		"test": "jest --config jestconfig.json",
		"build": "tsc",
		"format": "prettier -w .",
		"lint": "tslint -p tsconfig.json",
		"prepublishOnly": "npm test",
		"version": "npm run format && git add -A src",
		"postversion": "git push && git push --tags"
	},
	"files": [
		"lib/**/*"
	],
	"repository": {
		"type": "git",
		"url": "git+https://github.com/hartmann-jonas/pharos-designer.git"
	},
	"keywords": [
		"pharos",
		"designer2"
	],
	"author": "Jonas Hartmann",
	"license": "ISC",
	"bugs": {
		"url": "https://github.com/hartmann-jonas/pharos-designer/issues"
	},
	"homepage": "https://github.com/hartmann-jonas/pharos-designer#readme",
	"devDependencies": {
		"@types/jest": "^29.5.2",
		"jest": "^29.5.0",
		"prettier": "^2.8.8",
		"ts-jest": "^29.1.0",
		"typescript": "^5.1.3"
	},
	"dependencies": {
		"isip": "^0.0.1"
	}
}
